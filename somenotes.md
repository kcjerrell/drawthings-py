<!-- fullWidth: false tocVisible: false tableWrap: true -->
rename config dicts

identify duplicates

expose

add a way to combine them (I'm thinking reqbuilder can take a config dict or a list of config dicts)
nevermind you can just | them